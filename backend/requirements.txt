django==5.0.7
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1

psycopg2-binary==2.9.9
django-mptt==0.15.0
django-cors-headers==4.5.0

redis==5.0.7
django-redis==5.4.0

celery==5.4.0
requests==2.32.3
python-dotenv==1.0.1

# Realtime stack
channels==4.1.0
channels-redis==4.2.0
# ASGI server for Channels
daphne==4.1.0

# Optional: keep if you also run uvicorn for other apps
uvicorn==0.30.0

# (Optional) Enable HTTP/2 + TLS support in Daphne logs
# Twisted extras provide http2 + tls; safe to add
Twisted[tls,http2]
service_identity
